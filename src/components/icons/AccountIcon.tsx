import { User } from "lucide-react-native";

export function AccountIcon({
  size = 22,
  color = "#EEF3FF",
}: {
  size?: number;
  color?: string;
}) {
  return <User size={size} color={color} />;
}







